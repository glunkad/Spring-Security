spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/security_db
    username: gunjan
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:

    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true